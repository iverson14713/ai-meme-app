# Vercel serverless only — never expose in frontend bundles
OPENAI_API_KEY=

# Optional: override API origin for Capacitor builds (defaults to production Vercel URL on native)
# VITE_API_BASE_URL=https://ai-meme-app.vercel.app
