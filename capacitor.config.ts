import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.aimouthy.app',
  appName: 'AI有點嘴',
  webDir: 'dist',
  ios: {
    contentInset: 'automatic',
  },
}

export default config
